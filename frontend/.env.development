# Axios base URL. Defaults to "/api" (served by the Vite dev proxy → http://localhost:8080).
# Override to call a backend directly (requires CORS enabled there), e.g.:
# VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=/api
